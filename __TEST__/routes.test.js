const supertest = require("supertest");

const request = require('supertest');
const server = require('../api/server');


describe('server.js', () => {
    it('check - register endpoint', async () => {
        
    })
    it.todo('check - test login endpoints')
    it.todo('check - jokes endpoint')
})